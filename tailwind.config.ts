import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // React + TSX files
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;